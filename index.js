const chalk = require("chalk");
console.log(
  `${chalk.yellow(
    "Hello friend"
  )}. This is a placeholder library. You meant to install ${chalk.blue(
    "laf-lib"
  )} \n 
  ${chalk.green("npm install laf-lib -S")}`
);
